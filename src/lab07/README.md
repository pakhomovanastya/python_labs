Лабораторная работа №7
=
tests/test_text.py:
-
Тестирование функции normalize()
>Что делает функция: Разбивает текст на отдельные слова (токены).
>
>Функция должна уметь обрабатывать разные типы текста - отделять слова от знаков препинания, работать со сложными словами через дефис, игнорировать эмодзи.
>
>@pytest.mark.parametrize - это "декоратор", который позволяет запустить один тест с разными данными
>
>"source, expected" - названия переменных для входных данных
>
>Список кортежей [("вход1", "ожидание1"), ("вход2", "ожидание2")] - тестовые данные

 ```python
@pytest.mark.parametrize(
    "source, expected",
    [
        ("ПрИвЕт\nМИр\t", "привет мир"),
        ("ёжик, Ёлка", "ежик, елка"),
        ("Hello\r\nWorld", "hello world"),
        ("  двойные   пробелы  ", "двойные пробелы"),
        ("", ""),
        ("   ", ""),
    ],
)
def test_normalize(source, expected):
    "Тестирование функции normalize"
    assert normalize(source) == expected
 ```
 Тестовые случаи:
>("ПрИвЕт\nМИр\t", "привет мир") - проверяем, что функция:
>Переводит буквы в нижний регистр ("ПрИвЕт" → "привет")
>Заменяет символы \n и \t на пробелы
>("ёжик, Ёлка", "ежик, елка") - проверяем букву "ё" → "е"
>("Hello\r\nWorld", "hello world") - проверяем английский текст и символы \r\n
>(" двойные пробелы ", "двойные пробелы") - убирает лишние пробелы
>("", "") - пустая строка остается пустой
>(" ", "") - строка только из пробелов становится пустой
>assert normalize(source) == expected - это проверка:
>Вызываем функцию normalize с тестовыми данными
>Сравниваем результат с ожидаемым значением
>Если не совпадает - тест проваливается

Тестирование функции tokenize()
>@pytest.mark.parametrize - это "декоратор", который
>позволяет запустить один тест с разными данными
>
>"source, expected" - названия переменных для входных
данных
>
>Список кортежей 
>[("вход1", "ожидание1"), ("вход2", "ожидание2")] - тестовые данные

```python
@pytest.mark.parametrize(
    "source, expected",
    [
        ("ПрИвЕт\nМИр\t", "привет мир"),
        ("ёжик, Ёлка", "ежик, елка"),
        ("Hello\r\nWorld", "hello world"),
        ("  двойные   пробелы  ", "двойные пробелы"),
        ("", ""),
        ("   ", ""),
    ],
)
def test_normalize(source, expected):
    "Тестирование функции normalize"
    assert normalize(source) == expected
```
Тестовые случаи:
>("ПрИвЕт\nМИр\t", "привет мир") - проверяем, что функция:
>Переводит буквы в нижний регистр ("ПрИвЕт" → "привет")
>Заменяет символы \n и \t на пробелы
>("ёжик, Ёлка", "ежик, елка") - проверяем букву "ё" → "е"
>("Hello\r\nWorld", "hello world") - проверяем английский текст и символы \r\n
>(" двойные пробелы ", "двойные пробелы") - убирает лишние пробелы
>("", "") - пустая строка остается пустой
>(" ", "") - строка только из пробелов становится пустой
>assert normalize(source) == expected - это проверка:
>Вызываем функцию normalize с тестовыми данными
>Сравниваем результат с ожидаемым значением
>Если не совпадает - тест проваливается

Тестирование функции tokenize()
```python
@pytest.mark.parametrize(
    "source, expected",
    [
        ("привет мир", ["привет", "мир"]),
        ("hello,world!!!", ["hello", "world"]),
        ("по-настоящему круто", ["по-настоящему", "круто"]),
        ("2025 год", ["2025", "год"]),
        ("emoji 😀 не слово", ["emoji", "не", "слово"]),
        ("", []),
        ("!!! ???", []),
    ],
)
def test_tokenize(source, expected):
    "Тестирование функции tokenize"
    assert tokenize(source) == expected
```
Пояснение тестовых случаев:

>("привет мир", ["привет", "мир"]) - обычное разделение слов по пробелам
>("hello,world!!!", ["hello", "world"]) - знаки препинания удаляются, слова разделяются
>("по-настоящему круто", ["по-настоящему", "круто"]) - слова с дефисом остаются целыми
>("2025 год", ["2025", "год"]) - числа считаются словами
>("emoji 😀 не слово", ["emoji", "не", "слово"]) - эмодзи игнорируются, остаются только слова
>("", []) - пустая строка → пустой список
>("!!! ???", []) - только знаки препинания → пустой список

Тестирование функции count_freq()
```python
@pytest.mark.parametrize(
    "source, expected",
    [
        (["a", "b", "a", "c", "b", "a"], {"a": 3, "b": 2, "c": 1}),
        (["bb", "aa", "bb", "aa", "cc"], {"aa": 2, "bb": 2, "cc": 1}),
        ([], {}),
        (["word"], {"word": 1}),
    ],
)
def test_count_freq(source, expected):
    "Тестирование функции count_freq"
    assert count_freq(source) == expected
```
Пояснение:

>Функция принимает список слов и возвращает словарь с частотой
>(["a", "b", "a", "c", "b", "a"], {"a": 3, "b": 2, "c": 1}):
>"a" встречается 3 раза
>"b" встречается 2 раза
>"c" встречается 1 раз
>(["bb", "aa", "bb", "aa", "cc"], {"aa": 2, "bb": 2, "cc": 1}) - проверяем разные слова
>([], {}) - пустой список → пустой словарь
>(["word"], {"word": 1}) - одно слово → частота 1

Тестирование функции top_n()
```python
@pytest.mark.parametrize(
    "source, expected",
    [
        (["a", "b", "a", "c", "b", "a", "b", "b"], [("b", 4), ("a", 3), ("c", 1)]),
        (["bb", "aa", "bb", "aa", "cc"], [("aa", 2), ("bb", 2), ("cc", 1)]),
        ([], []),
    ],
)
def test_top_n(source, expected):
    "Тестирование функции top_n"
    assert top_n(source) == expected
```
Пояснение:

Функция возвращает отсортированный список кортежей (слово, частота)

>(["a", "b", "a", "c", "b", "a", "b", "b"], [("b", 4), ("a", 3), ("c", 1)]):
>"b" - 4 раза (первое место)
>"a" - 3 раза (второе место)
>"c" - 1 раз (третье место)
>(["bb", "aa", "bb", "aa", "cc"], [("aa", 2), ("bb", 2), ("cc", 1)]) - при равной частоте сортировка по алфавиту

Файл test_json_csv.py
---
>def test_json_to_csv_roundtrip(tmp_path: Path):
>
>tmp_path - специальная фикстура pytest, создает временную папку
>
>После теста все файлы автоматически удаляются
>
>src = tmp_path / "people.json" - создаем путь к исходному JSON файлу
>
>tmp_path - временная папка
>
>/"people.json" - добавляем имя файла

>json.dumps() - преобразует Python-объект в JSON-строку
>
>ensure_ascii=False - разрешаем русские буквы
>
>indent=2 - красивый формат с отступами

```python
def test_json_to_csv_roundtrip(tmp_path: Path):
    """Позитивный тест: корректная конвертация JSON → CSV"""
    # tmp_path - встроенная фикстура pytest для работы с временными файлами
    # создаем пути к временным файлам
    src = tmp_path / "people.json"
    dst = tmp_path / "people.csv"
    data = [
        {"name": "Alice", "age": 22},
        {"name": "Bob", "age": 25},
    ]
    # записываем тестовые данные в JSON файл
    src.write_text(json.dumps(data, ensure_ascii=False, indent=2), encoding="utf-8")
    json_to_csv(str(src), str(dst))
    with dst.open(encoding="utf-8") as f:
        rows = list(csv.DictReader(f))  # читаем CSV как список словарей
    # проверяем утверждения
    assert len(rows) == 2
    assert {"name", "age"} <= set(rows[0].keys())
```
Негативные тесты для JSON → CSV

>with pytest.raises(ValueError): - говорим pytest: 
>
>"ожидаем, что внутри этого блока произойдет ошибка ValueError"
>
>Если ошибка НЕ произойдет - тест провалится
>
>Если произойдет ДРУГАЯ ошибка - тест тоже провалится

Тест с неправильной структурой JSON
```python
def test_json_to_csv_with_out_dict(tmp_path: Path):
    "Негативный тест: JSON с неправильной структурой данных"
    # JSON файл содержит список чисел вместо списка словарей
    src = tmp_path / "people.json"
    dst = tmp_path / "people.csv"
    src.write_text(
        json.dumps([1, 2, 3, 4], ensure_ascii=False, indent=2), encoding="utf-8"
    )
    with pytest.raises(ValueError):
        json_to_csv(str(src), str(dst))
```
Пояснение:
>Создаем JSON файл с числами [1, 2, 3, 4] вместо словарей
>
>Функция конвертации ожидает список словарей, поэтому должна выбросить ошибку

Тест с пустым JSON файлом
```python
def test_json_to_csv_empty(tmp_path: Path):
    "Негативный тест: обработка пустого JSON файла"
    src = tmp_path / "people.json"
    dst = tmp_path / "people.csv"
    src.write_text(json.dumps([], ensure_ascii=False, indent=2), encoding="utf-8")
    with pytest.raises(ValueError):
        json_to_csv(str(src), str(dst))
```

Пояснение:
>json.dumps([]) - создаем JSON с пустым списком
>
>Пустой файл не может быть корректно конвертирован в CSV

Тест с некорректным JSON синтаксисом
```python
def test_json_to_csv_incorrect_json(tmp_path: Path):
    "Негативный тест: некорректный синтаксис JSON файла"
    src = tmp_path / "people.json"
    dst = tmp_path / "people.csv"
    src.write_text("Одинцово", encoding="utf-8")  # Это не JSON!
    with pytest.raises(ValueError):
        json_to_csv(str(src), str(dst))
```
Пояснение:
>Записываем в файл просто текст "Одинцово" - это не валидный JSON
>
>Функция должна обнаружить синтаксическую ошибку

Тесты для CSV → JSON
```python
def test_csv_to_json_roundtrip(tmp_path: Path):
    """Позитивный тест: корректная конвертация CSV → JSON"""
    src = tmp_path / "people.csv"
    dst = tmp_path / "people.json"
    # создаем корректный CSV файл с заголовком и данными
    src.write_text("name,age,city\nAlice,22,SPB\nBob,25,Moscow")
    csv_to_json(str(src), str(dst))
    with dst.open(encoding="utf-8") as f:
        rows = json.load(f)
    assert len(rows) == 2
    assert {"name", "age", "city"} <= set(rows[0].keys())
```
Пояснение CSV формата:
>Первая строка: "name,age,city" - заголовки колонок
>
>Вторая строка: "Alice,22,SPB" - данные первой записи
>
>Третья строка: "Bob,25,Moscow" - данные второй записи

Негативные тесты для CSV → JSON
```python
def test_csv_to_json_wrong_file(tmp_path: Path):
    "Негативный тест: неверное расширение файла для CSV → JSON"
    with pytest.raises(ValueError):
        src = tmp_path / "people.txt"  # .txt вместо .csv
        dst = tmp_path / "people.json"
        csv_to_json(str(src), str(dst))
```

```python
def test_csv_to_json_none(tmp_path: Path):
    "Негативный тест: обработка отсутствующего CSV файла"
    with pytest.raises(FileNotFoundError):
        src = "people.csv"  # Файла не существует!
        dst = tmp_path / "people.json"
        csv_to_json(str(src), str(dst))
```

Здесь ожидаем FileNotFoundError - другая ошибка!
```python
def test_csv_to_json_empty(tmp_path: Path):
    "Негативный тест: обработка пустого CSV файла"
    src = tmp_path / "people.csv"
    dst = tmp_path / "people.json"
    src.write_text("", encoding="utf-8")  # Пустой файл
    with pytest.raises(ValueError):
        csv_to_json(str(src), str(dst))
```

Запуск тестов
---
```python
# Запустить все тесты
pytest

# Запустить тесты с подробной информацией
pytest -v

# Запустить тесты с измерением покрытия кода
pytest --cov

# Запустить конкретный файл с тестами
pytest tests/test_text.py

# Запустить конкретный тест
pytest tests/test_text.py::test_normalize
```

Файл test_text.py
---
<img width="735" height="887" alt="test_text_часть1" src="https://github.com/user-attachments/assets/db23d1cb-e8e0-4a6e-922b-7ad02677c416" />
<img width="883" height="647" alt="test_text_часть2" src="https://github.com/user-attachments/assets/f01650de-912d-4fd1-9fee-38ae639d792b" />

Файл test_json_csv.py
---
<img width="948" height="852" alt="test_json_csv_часть1" src="https://github.com/user-attachments/assets/f3ee1884-a519-4df8-b306-c06dc9827fcc" />
<img width="942" height="1015" alt="test_json_csv_часть2" src="https://github.com/user-attachments/assets/f47b4303-4243-4731-b4b7-c44d99403a2a" />
<img width="897" height="1016" alt="test_json_csv_часть3" src="https://github.com/user-attachments/assets/690d8c05-8e53-482c-a5a2-f097c567023d" />

Вывод
---
<img width="1478" height="1025" alt="вывод" src="https://github.com/user-attachments/assets/25240eb6-5866-4e72-a8a8-66a7749cc289" />
